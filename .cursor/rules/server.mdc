---
description: 
globs: 
alwaysApply: false
---
the app is running in docker containers via docker compose (see docker-compose.yml)

server is in server directory. uv is used to install packages.

client is in client directory. yarn is used to install packages and run the client.

formatting is done with ruff. i also have pre-commit installed (.pre-commit-config.yaml).

keep your changes to minimum, avoid duplication, don't add example files.

don't mix dataclasses and pydantic models