import { memo } from "react";

function EditorToolbarComponent() {
  // Placeholder for future editor toolbar functionality
  // This could include formatting buttons, query templates, shortcuts, etc.
  return null;
}

export const EditorToolbar = memo(EditorToolbarComponent);
